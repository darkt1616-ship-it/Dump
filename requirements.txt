# Dipendenze runtime effettivamente usate dal codice
click>=8.1.0        # interfaccia a riga di comando (main.py)
tqdm>=4.65.0        # barra di avanzamento (src/generator.py)
psutil>=5.9.0       # informazioni di sistema (src/utils.py)
pyyaml>=6.0         # lettura/scrittura configurazioni (src/utils.py)

# NB: per i test installare pytest (vedi setup.py -> extras_require["dev"])
